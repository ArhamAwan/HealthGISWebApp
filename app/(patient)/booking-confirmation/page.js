'use client';
// Booking confirmation page is currently unused.
// The booking flow now confirms directly and routes
// to /appointments, but this file is kept so it can be
// re-enabled easily in the future.

export default function BookingConfirmationPage() {
  return null;
}
